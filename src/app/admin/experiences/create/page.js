import ExperienceForm from "../ExperienceForm";

export const metadata = {
  title: "Tambah Pengalaman | Admin CMS",
};

export default function CreateExperiencePage() {
  return <ExperienceForm />;
}
